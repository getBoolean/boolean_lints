include: package:lints/recommended.yaml

analyzer:
  plugins:
    - custom_lint

custom_lint:
  rules:
    - avoid_hardcoded_strings: true

boolean_lints:
  rules_exclude:
    - ".*excluded\\.dart"
  rules:
    - avoid_hardcoded_strings:
        - minimum_length: 3
        - severity: warning
